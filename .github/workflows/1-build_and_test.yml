name: Maven Package

on:
  push:
    branches:
      - jenkins-testing

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      # Checkout the code
      - name: Checkout Code
        uses: actions/checkout@v3
      
      - name: Run tests and build maven with production environment variables
        run: mvn clean package -Pprod
